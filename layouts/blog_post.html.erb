<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset='utf-8'>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= current_page.data.fetch("title", "Industrially.net") %></title>
    <script src="https://cdn.tailwindcss.com?plugins=typography"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#1E3A8A',
              accent: '#3B82F6',
              background: '#EFF6FF',
              text: '#1F2937',
            },
            fontFamily: {
              sans: ['Inter', 'sans-serif'],
            },
            typography: {
              DEFAULT: {
                css: {
                  maxWidth: 'none',
                  color: '#1F2937',
                  a: {
                    color: '#3B82F6',
                    '&:hover': {
                      color: '#1E3A8A',
                    },
                  },
                },
              },
            },
          },
        },
      }
    </script>
  </head>
  <body class="bg-white font-sans text-text">
    <header class="bg-white shadow-sm">
      <nav class="container mx-auto px-4 py-3 flex justify-between items-center">
        <a href="/" class="text-xl font-bold text-primary">Industrially.net</a>
        <a href="/blog/un-proceso-para-dar-seguimiento-a-cotizaciones" class="text-accent hover:text-primary">Blog</a>
      </nav>
    </header> 

    <main class="container mx-auto px-4 py-8 max-w-3xl">
      <article class="prose prose-sm sm:prose lg:prose-lg mx-auto">
        <h1 class="text-text text-2xl sm:text-3xl lg:text-4xl"><%= current_page.data.title %></h1>
        <p class="text-text opacity-75 text-sm sm:text-base"><%= current_page.data.date.strftime("%B %d, %Y") %></p>
        <%= yield %>
      </article>
    </main>

    <footer class="bg-gray-100 py-10">
      <div class="container mx-auto px-4 text-center">
        <h2 class="text-2xl font-bold mb-4 text-text">¿Quieres optimizar tus ventas?</h2>
        <p class="mb-4 text-text">Suscríbete para recibir noticias sobre nuestra app de seguimiento de cotizaciones.</p>
        <a href="/" class="bg-accent hover:bg-primary text-white font-bold py-2 px-4 rounded transition duration-300">
          Suscribirse
        </a>
      </div>
    </footer>
  </body>
</html>
