---
title: Industrially Blog | Aprende a usar software para potenciar tu negocio de distribución industrial
description: "Aquí compartimos pequeños tips, consejos y herramientas sobre todo relacionadas al uso de software en negocios de distribución y ventas de equipo industrial."
---

<div class="bg-white">
  <main class="container mx-auto px-4 py-8 max-w-2xl mb-10">
    <div class="mx-auto flex flex-col gap-6 sm:gap-12">
      <div class="flex flex-col gap-2">
        <h1 class="text-text text-2xl sm:text-4xl font-bold">Blog</h1>
      </div>
      <% site = Sitepress::Site.new %>
      <% posts = site.resources.glob("blog/*html*").select { _1.data.date.to_date <= Date.current }.sort_by { _1.data.date.to_date }.reverse %>
      <% posts.each do |post| %>
        <a href="<%= post.request_path %>" class="p-4 sm:p-6 border border-gray-200 bg-white rounded-xl shadow-lg flex flex-col gap-2 hover:border-gray-300 transition-colors duration-300">
          <div class="prose prose-sm sm:prose lg:prose-lg">
            <h3 class="mb-2"><%= post.data[:title] %></h3>
            <p class="text-gray-500"><%= post.data[:description] %></p>
            <%= image_tag post.data[:image], class: "w-full h-auto" if post.data[:image] %>
          </div>
        </a>
      <% end %>
    </div>
  </main>
</div>
